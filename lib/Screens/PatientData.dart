import 'package:flutter/material.dart';

class PatientData extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Text("Tela de PatientData")],
      ),
    );
  }
}