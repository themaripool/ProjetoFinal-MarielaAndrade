import 'package:flutter/material.dart';

class PatientSymptoms extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Text("Tela de PatientSymptoms")],
      ),
    );
  }
}